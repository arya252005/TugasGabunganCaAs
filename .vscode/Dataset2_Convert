!pip install micromlgen

import joblib
from micromlgen import port
from sklearn.ensemble import RandomForestRegressor


try:
 
    model_aktif = joblib.load('model_final.pkl')
    print("✓ Model berhasil dimuat dari file .pkl")

 
    header_content = port(model_aktif)

    with open("model_local.h", "w") as f:
        f.write(header_content)
    print("✓ File 'model_local.h' BERHASIL dibuat! Silakan cek folder kiri.")

except FileNotFoundError:
    print("✗ Error: File 'model_prediksi_waktu.pkl' tidak ditemukan di folder.")
